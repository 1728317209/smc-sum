import React from 'react';

const Title = ({ name }) => (
  <div style={{ textAlign: 'center', fontSize: '40px', fontWeight: 'bold', color: '#F3F3F3' }}>
    {name}
  </div>
);

export default Title;
